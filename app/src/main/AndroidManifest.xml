<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.sport4party">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-feature
        android:name="android.hardware.camera.any"
        android:required="true" />
    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".InformacionLugar"></activity>
        <activity android:name=".InformacionEvento" />
        <activity android:name=".CrearEvento" />
        <activity android:name=".Perfil" android:windowSoftInputMode="adjustNothing"/>
        <activity android:name=".Registro" />
        <activity android:name=".Mapa" />
        <activity android:name=".misEventos" />
        <activity
            android:name=".InicioDeSesión"
            android:theme="@style/AppTheme.NoActionBar.Custom">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".VerParticipantes"
            android:parentActivityName=".InformacionEvento" />
        <activity
            android:name=".InvitarAmigos"
            android:parentActivityName=".VerParticipantes" />
        <activity
            android:name=".MisAmigos"
            android:parentActivityName=".Mapa" />
    </application>

</manifest>