<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.sport4party">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

      
        <activity android:name=".InformacionEvento"></activity>
        <activity android:name=".CrearEvento" />
        <activity android:name=".Perfil"></activity>
        <activity android:name=".Registro" />
        <activity android:name=".InicioDeSesión" />
        <activity android:name=".Mapa">
            android:label="@string/title_activity_mapa"
            android:theme="@style/AppTheme.NoActionBar"
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
          
        </activity>
        <activity
            android:name=".VerParticipantes"
            android:parentActivityName=".Mapa" />
        <activity
            android:name=".InvitarAmigos"
            android:parentActivityName=".VerParticipantes" />
        <activity
            android:name=".MisAmigos"
            android:parentActivityName=".Mapa" />
    </application>

</manifest>